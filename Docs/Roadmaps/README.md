# Docs / Roadmaps — Mini Index

Status consolidado (U1): concluídos `RM_InteligenciaSistemica`, `RM_PrototypePlayable_P1` e `RM_StatusAndModifiers_P2` (Dia 1), com systems de base já fechados (`ActionGate`, `Attributes`, `Movement`, `Camera`, `UI_HUD`, `AvatarBridge`, `AnimBridge`, `Status`, `Modifiers`).

Próximo ciclo recomendado: iniciar planejamento/executar U2 por prioridade de gameplay (ex.: Movement/Status/Modifiers) mantendo a disciplina `build + conformance + smoke + evidências em MD` por etapa.
